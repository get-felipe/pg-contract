version: "0.2"

defaults:
  prepare:
    search_path:
      - public

query_sets:
  - name: app
    queries:
      - queries
    schema:
      before:
        - schema-before.sql
      after:
        - schema-after.sql
    tags:
      - example
      - manifest-v02

queries:
  customers.find_customer:
    tags:
      - customer-facing
